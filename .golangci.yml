run:
  timeout: 3m
  modules-download-mode: readonly
  skip-dirs:
    - sample

linters:
  # enable additional linters, that are disabled by default
  enable:
    - godot
    - gofmt
    - goimports
    - misspell
    - whitespace

